<% provide(:title, t(".title")) %>
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="main-box clearfix">
        <div class="table-responsive">
          <div class="panel panel-default">
            <div class="panel-heading panel-heading-leagues">
              <%= t ".title" %>
              <a href="<%= new_admin_team_path %>"><i class="fa fa-plus-circle" aria-hidden="true"></i> </a>
            </div>
            <div class="panel-body">
              <table class="table user-list leagues-list">
                <thead>
                  <tr>
                    <th><span><%= t ".name" %></span></th>
                    <th><span><%= t ".logo" %></span></th>
                    <th><span><%= t ".create_at" %></span></th>
                    <th><span><%= t ".members" %></span></th>
                    <th><span><%= t ".manage" %></span></th>
                  </tr>
                </thead>
                <tbody>
                  <%= render @teams %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <% if @teams.present? %>
    <section class="bottom-border"></section>
    <div class="digg_pagination">
      <%= will_paginate @teams, container: false %>
    </div>
  <% end %>
</div>
